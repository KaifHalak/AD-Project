/**
 * Placeholder accounts page shown after successful login.
 * This is intentionally empty for now.
 */
export default function AccountsPage() {
  return <main className="min-h-screen bg-background-main" />;
}
